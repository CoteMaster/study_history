#include<iostream>
#include<algorithm>
#include<cstring>

using namespace std;
char n[10];
int main(void){
    cin>>n;
    sort(n, n+strlen(n), greater<char>()); //내림차순
    for(int i=0; i<strlen(n); i++){
        cout<<n[i];    
    }
    return 0;
}
